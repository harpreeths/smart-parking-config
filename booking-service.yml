server:
  port: 8082

spring:
  application:
    name: BOOKING-SERVICE

  datasource:
    url: jdbc:postgresql://localhost:3320/bookingdb
    username: postgres
    password: BMT*cb^888
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
